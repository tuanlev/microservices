spring:
    application:
        name: auth-service
# Mysql
    datasource:
        urrl: jdbc:mysql://localhost:3306/pnj
        username: 
        password: 
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQL8Dialect
                format_sql: true
                use_sql_comments: true

    sql:
        init:
            schema-locations: lasspath:schema.sql
